# Info.plist Configuration
# ========================
# 
# DO NOT add this as a file to your Xcode project.
# Xcode auto-generates Info.plist — adding a second one causes build errors.
#
# Instead, add these keys via:
#   Target → Info tab → Custom iOS Target Properties
#
# Or add them to your target's build settings under:
#   INFOPLIST_KEY_... settings
#
# Required Privacy Keys (add in Info tab):
# ----------------------------------------
# NSPhotoLibraryUsageDescription = "Recipe Vault needs access to your photos to import recipe images and save photos of your cooked meals."
# NSCameraUsageDescription = "Recipe Vault uses your camera to photograph recipe cards and your finished dishes."
#
# Document Types (add in Info tab → Document Types):
# --------------------------------------------------
# Name: PDF Document
#   Types: com.adobe.pdf
#   Role: Viewer
#
# Name: JSON Document  
#   Types: public.json
#   Role: Viewer
#
# These allow the app to appear in the share sheet when sharing PDF/JSON files.
#
# The Claude API (api.anthropic.com) uses HTTPS so no ATS exceptions are needed.
